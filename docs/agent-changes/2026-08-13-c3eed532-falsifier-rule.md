- **`/decide` skill amended: Falsifier line required in ADR template (task c3eed532, ADR-0098)**: added a required `**Falsifier:**` bullet to the `### Risks` section of the ADR template — the concrete observable symptom that would show the system no longer honours the decision, distinct from restating it. Added one question to the reviewer checklist: "Does this ADR name a falsifier — the symptom a reader could witness — distinct from a restatement of the decision?" The Check mandate (binding a deterministic script to each Falsifier) is deferred to ADR-0099 (proposed); no `Check:` line added to the template. PR: to be added on merge.
