- [#410](https://github.com/joeLepper/treadmill/pull/410) — ADR-0116 (amends 0110/0115): GATE WEIGHT MATCHES BLAST RADIUS. In `merge_target=feature-branch` mode the coordinator (§9.1) briefs the evaluator with `panel_breadth: single`, so the ADR-0108 evaluator runs a SINGLE cross-model pass (`panel.py --min-cross-model 1` — one cross-family voice, unbiased vs the same-family implementer) + KEEPS the §9.4/§9.5 rework loop; the FULL cross-model panel (`--min-cross-model 2`) + full CI run ONCE at the integration→`main` promotion (or the human's review of a non-`main`-base deliverable, ADR-0114). Main-merge mode is unchanged (`panel_breadth: full`). Templates: `coordinator/CLAUDE.md.tmpl` §9.1, `evaluator/CLAUDE.md.tmpl`. Motivated by the netlify dogfood: a docs-only finding ran the full panel + up-to-3 rework cycles (~1hr), yet the evaluator caught a real correctness defect peer-review missed — so lighten the panel breadth, not the evaluator or the loop. Foil: `test_adr0116_panel_breadth_matches_blast_radius`.
