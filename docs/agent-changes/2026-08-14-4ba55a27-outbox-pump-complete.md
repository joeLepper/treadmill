- **[#PR] Outbox pump — completing slice-1a (task 4ba55a27, ADR-0094)**: adds `services/api/tests/test_outbox_pump_complete.py` with three load-bearing tests (evaluator-required, non-vacuous, red-then-green). (1) SINGLE-PUMP FLOCK REFUSAL: pump1 acquires flock and drains all rows; pump2 on the same lock file refuses (`run()` == -1, publishes nothing) — both halves asserted in one test. (2) WRITE-THEN-CRASH-BEFORE-PUBLISH REDELIVERS with foil: RED — `_MarkBeforePublishPump` marks row 1 published before calling publisher; publisher raises; row 1 permanently marked and silently lost; recovery pump delivers only rows 2–3. GREEN — correct `OutboxPump` never marks before publish; a crash (pump never ran) leaves all rows pending; recovery pump delivers all 3 in order. (3) DRAIN-ON-RECONNECT IN ORDER: 7 rows accumulate while pump is not running (simulating host network outage); pump on reconnect delivers all 7 to the publisher in append order, none dropped. Updates `treadmill_api/messaging/AGENT.md` navigation to list `test_outbox_pump_complete.py` and documents the completing semantics.
