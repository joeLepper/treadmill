- **New `.claude/skills/release/SKILL.md` skill (task d7ff2ae9, ADR-0096)**: added the `release` skill, encoding the four rules from ADR-0096: (1) code reaches a host only via a release built from main, (2) a host adopts a release by restart (ADR-0073 systemd-per-label substrate), (3) hot-loading is prohibited (importlib.reload, code hot-load, monkeypatching), (4) restart is the unit of adoption and can never regress the runtime. PR: to be added on merge.
  Supersedes ADR-0024 (deploy-watcher merge-triggered redeploy) and ADR-0069 (self-heal re-exec on stale code fingerprint) as the routine deployment path; those ADRs describe mechanisms that are no longer the canonical path under ADR-0096.
