# 2026-09-15 — ADR-0118 phase 2: SubprocessGitRunner (prod merge executor)

- `coordination/git_runner.py`: `SubprocessGitRunner` (real `git` in a working clone,
  implements the merger's `GitRunner` protocol; stderr folded into stdout for the merger's
  conflict-vs-infra read; timeout + git-not-found handling) and `ensure_working_clone` (one
  long-lived clone per repo under a router state dir, reused across ops; ambient git/gh creds,
  no new secret handling).
- `tests/test_integration_git_merge.py`: REAL-git end-to-end (local bare origin + clone, no
  GitHub) — `integrate_task` through the real runner lands the task head on origin's
  integration branch, and a second run is idempotent (`already-integrated`) via the ancestry
  check on real refs. 11 green (this + the 10 scripted foils).
- PENDING: fetch the PR ref (refs/pull/<n>/head) over a bare SHA when the MergeOp carries the
  PR number (Bert #2); wiring integrate_task to the approve verdict.
