- **[#PR] Ported ramjac-events invariant tests (task f6db308b, plan 6791c1cd)**: adds `tests/test_messaging_invariants.py`. Hardens the existing package with two red-then-green foils and 6 claim/commit/expiry unit tests, all ported from ramjac-events with provenance comments. Foil (1): `_MarkThenProcessCrashingConsumer` (commit before handle) — silently loses the message after crash; correct `MessageConsumer` redelivers and processes exactly once. Foil (2): `_UnscopedDedupConsumer` (shared `consumer_id`) — blocks subscriber-B after subscriber-A commits; correct per-`consumer_id` scoping gives each subscriber exactly one delivery.
