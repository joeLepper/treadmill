- **[#PR] Outbox pump — completing slice-1a (task 4ba55a27, ADR-0094)**: adds `tests/test_outbox_pump_complete.py` with three load-bearing tests. (1) SINGLE-PUMP FLOCK REFUSAL — first pump acquires flock and drains; second pump refuses (`run()` == -1). (2) WRITE-THEN-CRASH-BEFORE-PUBLISH REDELIVERS — RED foil `_MarkBeforePublishPump` marks before publish; crash silently loses row 1; GREEN correct pump leaves all rows pending after crash; recovery delivers all in order. (3) DRAIN-ON-RECONNECT IN ORDER — 7 rows accumulate while pump not running; pump on reconnect delivers all in append order (ADR-0094 §2: retries indefinitely, never drops).
