- **[#PR] Broadcast — channel subscription with per-subscriber dedup (task dc40b385, ADR-0093)**: adds `broadcast.py` — `ChannelStore` records join/leave events with monotonic offsets; `subscribers_at(channel, up_to_offset)` folds to the subscriber set at any broadcast offset. `fanout()` expands to N unicast deliveries: composite dedupKey `f"{subscriber}:{broadcast_id}"`, ordering_key=subscriber (per-subscriber order), emit() injected. Nine tests in `test_messaging_broadcast.py`: ONCE-PER-SUBSCRIBER foil-a (fresh UUID → re-fanout re-delivers), foil-b (broadcast_id alone + shared dedup → first-to-dedup wins), green (composite key + per-subscriber dedup → once each, re-fanout blocked). ISOLATED-AT-BROADCAST, JOIN-AFTER, LEAVE-AFTER. Fold unit tests. Broadcast section added to AGENT.md (three rules + production wiring note). Provenance: ramjac ADR-0009.
