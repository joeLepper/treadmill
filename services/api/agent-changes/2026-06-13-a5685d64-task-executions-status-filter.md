- **[#353](https://github.com/joeLepper/treadmill/pull/353) task_executions GET ?status= filter + reconcile endpoint (task a5685d64)**: `GET /api/v1/task_executions` now accepts an optional `?status=running|completed|failed` query param; previously the param was silently ignored, causing the coordinator's startup stale-sweep to fetch all 102 historical rows and mark them `failed/coordinator_restart` (ADR-0087 §2.1 bug). `POST /api/v1/task_executions/reconcile-coordinator-restart` restores rows wrongly mis-marked (`status='failed'`, `failure_reason='coordinator_restart'`) on tasks now in a terminal good state (`pr_merged`/`done`/`cancelled`) back to `completed`; idempotent. Tests in `test_routers_task_executions.py` (+7).
