- **[PR pending] scheduler decision ranked list (task c31e0994)**: `GET /api/v1/scheduler/decision` now returns `desired_teams: list[str]` (the FULL ranked list of teams with pending work, highest score first) instead of `desired_team: str | None` (argmax only). The scoring/aging formula (`score = pending_tasks + wait_min / AGING_T`) and quiescence logic are byte-for-byte identical; only the return value changes. The daemon will apply its own N-cap and subscription-pool logic to this list (tasks 6c2446b2 + bc5cdc23 own the cap/pool side). `SchedulerDecisionResponse` and `Decision` dataclass updated accordingly. `test_scheduler_decision.py` updated: all `desired_team` → `desired_teams` assertions, new `test_ranked_list_contains_all_pending_teams_highest_first` verifying both teams with pending work appear in order with idle teams excluded. Module docstring updated to describe the ranked-list contract. No daemon-side changes.
