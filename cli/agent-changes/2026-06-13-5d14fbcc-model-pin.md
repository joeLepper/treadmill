- **Per-role ANTHROPIC_MODEL pin in per-label .env (task 5d14fbcc — INCIDENT 2026-06-12)**: `_env_contents()` in `commands/team.py` now emits `ANTHROPIC_MODEL=<model>` for every role. New module-level `_ROLE_MODEL` dict maps `coordinator` and `evaluator` to `claude-opus-4-8`, `worker` to `claude-sonnet-4-6`. The launcher sources the per-label `.env` with `set -a` so the var reaches the subprocess. **Why env-var and not settings.json**: settings.json `"model"` does NOT override a persisted Fable-5 selection on `--resume`; only the subprocess env path reaches the session reliably. Root cause: model-less sessions default to `claude-fable-5` (unavailable on this account) on restart; coordinator + evaluator sessions silently died in the 2026-06-12 incident (workers were safe only because their cwd `.claude/settings.json` already pinned sonnet). Tests: `TestModelPin` class in `tests/test_cli_team_up.py` (4 tests — one per role + a cross-role distinction check asserting opus and sonnet appear in different roles).
