# tools/local-adapter

## Purpose

This directory contains the Treadmill-native local adapter, the bridge between CDK synth output and a moto + native Docker substrate for local-first development. It reads the CloudFormation JSON synthesized by the CDK app and operationalizes it by provisioning AWS-managed primitives (SNS, SQS, S3, IAM, Secrets, SSM) against moto, launching Postgres and Redis as native containers, wiring ECS task definitions to docker run commands, and running an autoscaling control loop that matches SQS queue depth to container count. The adapter ensures local behavior is faithful to AWS production for the cases developers care about: service definitions, scaling policies, credential injection, and the exit-then-restart cycle.

## Key surfaces

- `treadmill_local/cli.py` — entry point; `treadmill-local up` brings the substrate online, `down` tears it down, `logs` streams container output. The `repo onboard` subcommand registers the cwd's repo with the running deployment per ADR-0051.
- `treadmill_local/runtime.py` — core orchestrator; loads CDK JSON, dispatches to provisioners, starts containers, runs the autoscaler loop. `_run_container` accepts an optional `docker_adapter` (ADR-0060): when provided, worker containers are spawned on the `treadmill-egress` network with proxy env vars and per-worker install credentials injected. `_up_dev_local` calls `ensure_egress_network` (ADR-0064 Step 1) before `_start_services` so the egress network exists when the API spawns and can be multi-attached to it. `_start_services` + `recreate_api_container` then call `_multi_attach_api_to_egress` after the API container is up (ADR-0064 Step 2) so workers on the internal-only egress bridge can resolve `treadmill-api` by DNS.
- `treadmill_local/provisioner.py` — provisions AWS primitives (SNS, SQS, S3, IAM, Secrets, SSM) into moto.
- `treadmill_local/autoscaler.py` — target-tracking control loop; reads queue depth from the API (`get_depth_from_api`, ADR-0085+0086 Task E — `GET /api/v1/queue_depth` excludes tasks whose `created_by` matches a registered coordinator label, so coordinator-emitted brief tasks don't inflate the scaling signal), docker stats from Docker daemon, computes desired worker count from scaling policy, launches/drains containers. The original SQS-direct closure is preserved as `get_depth_sqs` for reference + debugging. An `httpx.Client` is constructed once at `run_autoscaler` scope and shared by `heartbeat_fn_impl` + the `queue_depth_fn` lambda (connection pooling across ticks); closed via `try/finally` around `autoscaler.run()`. On any failure (timeout, non-2xx, JSON parse), `get_depth_from_api` logs WARNING and returns `(0, 0)` so the autoscaler scales down to `min_count` rather than crash-looping. On startup, ensures the `treadmill-egress` network and proxy container exist (ADR-0060); passes `DockerClientAdapter` to each worker spawn for egress isolation. The `TREADMILL_EGRESS_PROXY_ENABLED` env var (default `true` as of ADR-0064 Step 3) gates this whole path; setting it to `false` is the operator escape hatch for debugging the proxy itself (suspected proxy bug, crash-looping proxy container, allowlist misconfiguration needing the unproxied path to bisect against).
- `treadmill_local/docker_client.py` — thin `DockerClientAdapter` wrapping docker-py for dependency injection; all egress-proxy Docker interactions go through this adapter so tests can substitute a fake without a real daemon. Includes `connect_container_to_network` (ADR-0064 Step 2) for multi-attaching an already-running container to a second network (idempotent — no-op when already attached).
- `treadmill_local/egress_proxy.py` — egress proxy lifecycle helpers (ADR-0060): `ensure_egress_network`, `ensure_egress_proxy_container` (idempotent — multi-attaches the proxy to `treadmill-local` per ADR-0064 Step 2 so outbound CONNECTs route through that network's gateway), `mint_worker_credential`, `build_always_allowed`, `build_install_allowed`, `write_worker_allowlist`.
- `treadmill_local/deployment_config.py` — writes `~/.treadmill/<deployment_id>.yaml` with container ports and endpoint URLs per ADR-0016's schema.
- `treadmill_local/subprocess_logging.py` — bounded logging helpers for the autoscaler + deploy-watcher subprocesses: a `configure_rotating_logging` setup (size-rotating file handler replacing stdout) and a `RateLimitedErrorLogger` that logs the first occurrence of an error in full and collapses repeats into periodic counted summaries. The scheduler subprocess uses the parallel implementation in `services/api/treadmill_api/scheduler/bounded_logging.py` (separate package; same shape but not shared by import).
- `treadmill_local/onboard.py` — pure helpers for client-side onboarding discovery (ADR-0051): `infer_repo` parses git remotes, `build_profile` produces a minimal `repo_profile` from the local checkout, `onboard_payload` assembles the POST body.
- `treadmill_local/docs_sync.py` — pure, testable doc sync helpers (ADR-0054): `list_docs`, `get_doc`, `pull`, `push`. Each function accepts injected HTTP callables so tests mock entirely without a network. `pull` downloads all docs from the API into a local directory; `push` uploads all files under a local directory to the API (last-write-wins, no-op when the dir is absent). The `docs` CLI command group wires these into `list` / `get` / `pull` / `push` subcommands, and **scopes the local mirror by repo**: `--dir` is a root and files land under `<dir>/<owner>/<name>/<doc_path>`, so multiple projects don't collide locally (mirrors the S3 `repo-context/<repo>/` layout).
- `scripts/smoke_boot.sh` — CI boot wrapper for the autoscaler smoke gate (ADR-0065). Parses `--port`, `--proxy-enabled`, `--timeout`; exports `TREADMILL_EGRESS_PROXY_ENABLED` and moto dummy creds; invokes `uv run treadmill-local up --no-build --no-autoscaler --no-scheduler --no-observability`; polls `http://localhost:<port>/healthz`; spawns one worker via a Python one-liner that calls `LocalRuntime.start_worker_once(AGENT_FAMILY)`. Emits `BOOT_READY` / `BOOT_FAILED` markers the GH Actions workflow keys off. Operators boot a local stack via `treadmill-local up` directly; this wrapper exists so CI can reuse the same boot procedure without re-implementing it inline in YAML.

## Recent changes

- **ADR-0091 team-scheduler daemon (task 9d6c0658 — the plan finale)**: new `treadmill_local/team_scheduler.py`, modeled on the deploy-watcher. Polls `GET /api/v1/scheduler/decision` and reconciles the running team set via `treadmill-team-control`: pause the current team ONLY when the API lists it quiescent, then activate the desired team (resume context is the launcher's `--resume` + the channel server's reconcile-on-connect `catch_up` — no daemon hook needed). LOAD-BEARING FAIL-SAFE: unreachable/error/malformed/null decision → HOLD, zero pause calls (the API is a SPOF; degrade to leave-things-as-they-are, pinned by tests). Four contractual review notes honored: the pause set structurally excludes `desired_team` (the endpoint reports facts, the daemon enacts policy); the anti-flap dwell (default 20 min, persisted stamp so restarts can't flap) is asserted `<= AGING_TIME_CONSTANT_MINUTES` against the IMPORTED constant in the suite; the wedged-coordinator >15 min half-registration edge is inherited knowingly (module docstring); slugs are case-normalized on intake both sides. Single-writer via a host-global flock under `~/.treadmill` (#333 guard class — kernel-released, no TOCTOU/PID-recycling). Wired into `runtime.py` as `start_team_scheduler` DEFAULT OFF (plan ships it disabled until the operator dry-runs; `up` prints the not-enabled note), with `_start/_stop/_pid_alive` mirroring the deploy-watcher trio incl. the recycled-pid identity guard. Tests `tests/test_team_scheduler.py` (15: fail-safe quartet, switch ordering, desired-team exclusion, single-active-over-progress, idle-fleet immediate activation, casing, dwell window + restart persistence, dwell<=aging direct import, flock refusal, label-shape enumeration). Real-systemd evidence in the PR: throwaway instance-specific units, real team-control, 4 reconcile passes + the flock singleton refusal.

- **Deploy-watcher single-instance lock + clean daemonization (task f82f7590 — the dual-watcher root cause behind the 2026-06-11 stale-recreate outages)**: the watcher pidfile lived under the CWD-RELATIVE `.treadmill-local/` state dir, so two worktrees each running `up` got two private pidfiles and two "legitimate" watchers racing to recreate the ONE shared `treadmill-api` container from different sources — #329's digest-pin guards image identity; this guards WATCHER identity. `deploy_watcher.main()` now acquires a host-global `flock` (`~/.treadmill/deploy-watcher.<deployment_id>.lock`) before ANY side effect; a second watcher logs the winner's diagnostics and exits 0 (refusal = the exactly-one invariant holding). flock is kernel-released on death — no TOCTOU, no PID-recycling class, no stale-lock recovery needed. The staleness re-exec releases the lock first via `_LockReleasingGuard` (flock conflicts across open descriptions even same-process; the fresh `main()` re-acquires; a racer winning the microscopic window means the re-exec'd loser exits — invariant still holds). Pidfile moved host-global alongside the lock (`watcher_pid_path`; legacy cwd-relative file still written/cleaned for one transition window); parent `_stop_/_pid_alive` walk global-then-legacy and gate kills on a `/proc/<pid>/cmdline` identity check (the #330 reap lesson — a recycled pid reads as a stale file, never a kill target). Spawn hardened: `stdin=DEVNULL`, `cwd=repo_root`. Tests: `tests/test_deploy_watcher_singleton.py` (real child interpreters acquiring the real flock: second-watcher refusal w/ winner diagnostics, release-on-death, dual-watcher race run-log, same-process reopen conflict, lock-releasing re-exec wrapper, identity-guard accept/reject/dead) + recycled-pid stop-path test; the two call-sequenced stop tests now pin HOME to the sandbox so they can never touch the live host's `~/.treadmill`.

- **Deploy-watcher recreate digest-pinned (task c62f097d — the highest-severity stale-recreate class, 2026-06-11 8h54m loop outage)**: `_action_api` now resolves the image ID of the build it JUST ran (`docker image inspect treadmill-api:dev --format {{.Id}}` immediately after `docker build`) and hands it to the recreate; `LocalRuntime.recreate_api_container(expected_image_id=…)` runs the new container FROM THAT ID (`dataclasses.replace` on the spec), never from whatever the mutable `:dev` tag resolves to at run time — so a migration-carrying merge can no longer boot pre-merge code against the post-merge schema (`alembic` Can't-locate-revision crashloop; the API is every consumer's SPOF). When the tag and the pinned ID disagree, the runtime logs both loudly — that warning is the smoking gun for the still-unidentified 06:52:50Z tag-repointing actor (the post-mortem's open follow-up). Operator paths without a freshly-built ID keep run-from-tag behavior (back-compat, pinned by test). Tests: deploy-watcher suite pins the inspect call + the pinned-ID pass-through (incl. the ff-only-fallback path); runtime suite pins run-by-ID, the stale-tag warning, and the no-pin default. Docker-level reproduction (stale tag boots pre-merge bits; pinned ID immune) in the PR run log.

- **ADR-0085+0086 Task E — autoscaler reads queue depth from API instead of SQS** (`treadmill_local/autoscaler.py`): renamed `get_depth` → `get_depth_sqs` (preserved verbatim for reference + debugging; no functional change). Added module-level `get_depth_from_api(api_base_url, client) -> (visible, in_flight)` that calls `GET /api/v1/queue_depth` against the API; the server-side SQL excludes tasks whose `created_by` matches a registered coordinator label (per Task C's `team_configs` registry) so coordinator-emitted brief tasks don't inflate the scaling signal. On any failure (HTTP error, timeout, connection refused, JSON parse) logs WARNING and returns `(0, 0)` so the autoscaler scales down to `min_count` rather than crash-loop on a transient API blip. INFO-level log on each successful call: `"queue_depth: %d visible, %d in_flight (coordinator-owned excluded)"`. `heartbeat_fn_impl` and the new `queue_depth_fn` lambda now share one `httpx.Client` constructed at `run_autoscaler` scope (connection pooling across ticks); cleanup via `try/finally` around `autoscaler.run()`. Tests in `tests/test_autoscaler.py`: existing `test_get_depth_closure_*` renamed to `test_get_depth_sqs_closure_*` to reflect the preserved-original symbol; three new `test_get_depth_from_api_*` cases (success returns parsed tuple + hits the right URL; HTTP error returns `(0, 0)` + logs WARNING; `httpx.ConnectError` returns `(0, 0)` + logs WARNING). All 54 tests pass. Out-of-scope (per brief): `team_configs`, `plans.py`, the CLI.

- PR — Autoscaler image-build fallback + `image_build_broken` escalation. `LocalRuntime(build_images=...)` flag now propagates to the autoscaler subprocess via `TREADMILL_AUTOSCALER_BUILD_IMAGES` env var (true|false, default true); `treadmill-local up --no-build` skips rebuilds in both the parent and the autoscaler child. The autoscaler tracks consecutive `_ensure_images_built` RuntimeErrors; after K=12 consecutive failures (~1 min at 5s tick), it runs ONE tick with `build_images=False` against the last-known-good image (via a fallback callable `start_worker_no_build_fn`), resets the counter, and logs the fallback explicitly. After F=3 consecutive fallback ticks without recovery, the autoscaler emits `{"image_build_broken": true}` via a new `heartbeat_fn` callback; the API server will emit a `system.image_build_broken` event (phase 2). Tests in `tests/test_autoscaler_fallback.py` cover all four cases: env var disables build, K failures triggers fallback, F fallbacks escalates, successful build resets counter. Closes the critical gap where `--no-build` had no effect inside the autoscaler.
- PR — ADR-0072: managed host processes read IAM credentials from a Treadmill-owned file, not operator SSO. New `treadmill_local/managed_credentials.py` defines `ManagedHostCredentials` (TypedDict schema), `ManagedCredentialsFileError` (loud failure exception), and `resolve_managed_host_credentials(path)` (returns env-overlay dict or None). Both `_start_autoscaler_dev_local` and `_start_deploy_watcher_dev_local` in `runtime.py` call the resolver after building the env dict: when a managed-credentials file is present, `AWS_PROFILE` is dropped and the IAM key vars are merged in; when the file is absent, the SSO path is unchanged; when the file exists but is malformed, spawn aborts loudly (never silently falls back). File `~/.treadmill/managed-host-credentials.json` is `.gitignore`-protected. Tests in `tests/test_managed_credentials.py` cover all three resolver outcomes plus the runtime env-merge and SSO-fallback paths via `tmp_path` + monkeypatched Popen.
- PR — ADR-0069: managed host processes self-heal on stale source. New `treadmill_local/staleness.py` defines `package_fingerprint` (sha256 over sorted `*.py` bytes), `StalenessGuard` (records the startup fingerprint + reports drift via `changed()`, calls `os.execv` via `reexec(pid_file)`), and a `maybe_reexec(guard, pid_file)` convenience. The autoscaler, scheduler, and deploy-watcher consult the guard at the TOP of their loops (safe re-exec point — no tick in flight, no event mid-handler) and re-exec when the watched package bytes have drifted; each subprocess's `main()` (re)writes its own PID file at startup so the parent's pid-file machinery keeps working across the exec. The deploy-watcher's `adapter` category is no longer notify-only — `_NOTIFY_ONLY_CATEGORIES` shrinks to `{"infra"}`, and `_action_adapter` now `_sync_local_to_origin`s the clone and then invokes the injected `restart_host_processes_fn` accelerator so a `tools/local-adapter/**` merge restarts the autoscaler + scheduler within the same watcher iteration instead of waiting on the operator. The watcher self-heals via its own staleness guard BEFORE running the accelerator, so a stale watcher heals first. `LocalRuntime.restart_host_processes` is the public hook (stop-then-start autoscaler + scheduler via the existing dev-local helpers); the wiring site in `deploy_watcher.main()` mirrors the existing `recreate_api_fn`/`recreate_dashboard_fn` callable-injection pattern. The scheduler runner defers its `treadmill_local.staleness` import to `_build_staleness_wiring()` so production deploys without the adapter on `PYTHONPATH` still boot (they just don't self-heal — same property production gets from container immutability). Tests: `tests/test_staleness.py` for the module, loop-head wiring tests appended to `test_autoscaler.py` / `test_deploy_watcher.py` / `services/api/tests/test_scheduler_runner.py`. **Bootstrap caveat: this change itself deploys only after a one-time manual restart of `treadmill-local up` — the running pre-ADR-0069 processes have no staleness guard, so the merge that introduces the guard cannot self-heal them. Subsequent `tools/local-adapter/**` merges propagate automatically.**
- PR — ADR-0065 Step 1: added `scripts/smoke_boot.sh`, the CI boot wrapper that drives the upcoming autoscaler real-Docker smoke gate. The script boots the minimal fully-local (moto-backed) service set so the gate runs with no SSO dependency, polls `/healthz`, then spawns one worker via `LocalRuntime.start_worker_once`. Flag parsing is covered by `tests/test_smoke_boot.py` using a stubbed `SMOKE_TREADMILL_CMD`; the real boot path needs Docker and is exercised by the workflow itself (Step 2). No CLI changes — `up` already supports `--no-build` / `--no-autoscaler` / `--no-scheduler` / `--no-observability`.
- PR (this change) — package registries (`INSTALL_DEFAULTS`: pypi.org, files.pythonhosted.org, registry.npmjs.org, registry.yarnpkg.com) are appended to `build_always_allowed()`, not just the install phase. Author-side validation gates run the standard `cd <pkg> && uv run pytest` pattern and `uv run` builds the project venv at invocation time — with registries phase-scoped, every uv-run gate failed (`tunnel error: unsuccessful` on wheel downloads) and tasks looped through gate-broken architect resolutions (task 57598e7a, 2026-06-04). No new trust: the same hosts were already install-phase-reachable. NOTE the stale-process trap — this takes effect only when the autoscaler process restarts (see `docs/learnings/2026-06-04-host-processes-pin-code-at-import-time.md`).
- PR — ADR-0064 Step 3: flipped `TREADMILL_EGRESS_PROXY_ENABLED` default from `false` to `true` in `autoscaler.py`. The cross-network DNS gap that originally kept ADR-0060 Step 3b feature-flagged off is closed by the ADR-0064 multi-attach (API + egress proxy live on both `treadmill-local` and `treadmill-egress`); the ADR-0065 real-boot smoke gate ran green against the new default before this ships. The flag stays as an operator escape hatch: set it to `false` to spawn workers on the operator-default network (no proxy, no per-worker isolation) when debugging the egress proxy itself — suspected proxy bug, crash-looping proxy container blocking all workers, or an allowlist misconfiguration that needs the unproxied path to bisect against. Inline comment above the env-read in `autoscaler.main` documents the new default + the escape-hatch conditions; `test_autoscaler.py` pins both the default-on case and the explicit-`false` escape hatch.
- PR — ADR-0064 Step 2: multi-attach the API + egress proxy across networks. `DockerClientAdapter.connect_container_to_network` (idempotent — short-circuits when the container already carries the network) joins the API container to `treadmill-egress` after spawn in both `_start_services` (initial boot) and `recreate_api_container` (deploy-watcher recreations); `ensure_egress_proxy_container` now joins the proxy to `treadmill-local` after spawn so its outbound CONNECTs route through that network's gateway (the egress bridge is `internal=True` and has no upstream). Closes the cross-network DNS gap that kept the ADR-0060 egress proxy feature-flagged off — workers on the internal-only egress bridge can now resolve `treadmill-api` for the installation-token mint at startup. New: `runtime.LocalRuntime._multi_attach_api_to_egress` helper; tests in `test_autoscaler.py` and `test_runtime_dev_local.py`.
- PR — ADR-0064 Step 1: `_up_dev_local` in `runtime.py` now calls `ensure_egress_network(DockerClientAdapter(self.docker))` just before `_start_services`, so the `treadmill-egress` Docker network exists at API spawn time. This is the foundation for later steps that multi-attach the API + egress proxy to both `treadmill-local` and `treadmill-egress` (closing the cross-network DNS gap that kept the ADR-0060 egress proxy feature-flagged off). The autoscaler's own `ensure_egress_network` call is unchanged — the helper is idempotent (the adapter catches `NotFound` on `get` and only creates on miss), so the standalone-autoscaler test path still works.
- PR — ADR-0060 Step 3b feature-flagged OFF by default. The egress proxy spawn / per-worker isolation network shipped in PR #92 has a cross-network DNS gap: workers on `treadmill-egress` (internal, no gateway) cannot resolve `treadmill-api` (lives on `treadmill-local`), so the worker startup installation-token mint fails with `Temporary failure in name resolution` and the worker exits 1 immediately. The proper fix is multi-attach the API to both networks (or move all internal services onto the egress network) plus a real end-to-end smoke test. Until that lands, the autoscaler checks `TREADMILL_EGRESS_PROXY_ENABLED` (default `false`); when off, workers spawn on the operator-default network like pre-#92 — no proxy, no per-worker isolation, but they actually run. Flip to `true` once the cross-network fix ships.
- PR — ADR-0060 Step 3b follow-up #2: workers now get `NO_PROXY=treadmill-api,localhost,127.0.0.1` in their spawn env so internal Treadmill-API calls bypass the egress proxy. The proxy is CONNECT-only (HTTPS); plain-HTTP traffic to `http://treadmill-api:8088` (e.g. the GitHub-App installation-token mint at worker startup) returned 400 if routed through it, causing every worker to exit code 1 immediately and the autoscaler to crashloop. The fix factors the env dict into `egress_proxy.worker_proxy_env(install_cred)` so the autoscaler call site and tests share one shape; covered by `tests/test_autoscaler.py::test_worker_proxy_env_bypasses_internal_hosts`.
- PR — ADR-0060 Step 3b follow-up: `_ensure_images_built` now builds a fourth image, `treadmill-egress-proxy:dev`, from `services/egress-proxy/Dockerfile`. Without this the autoscaler-spawn code from the original Step 3b 404'd on `docker pull` (image existed nowhere) and no workers spawned — the 2026-06-02 delivery gap. Build context is `services/egress-proxy/`; mirrors the api/dashboard pattern (cwd at the package dir, `docker build -t <image> .`). `DEV_LOCAL_EGRESS_PROXY_IMAGE` is declared next to the other dev-image constants in `runtime.py` so the build list and the spawn site reference the same tag.
- PR — ADR-0060 Step 3b: autoscaler now ensures a `treadmill-egress` internal bridge network and spawns the `treadmill-egress-proxy` container on startup. Each worker is spawned on that network (no external gateway) with `HTTP_PROXY`/`HTTPS_PROXY`/`TREADMILL_INSTALL_PROXY_TOKEN` env vars injected. A per-worker `WorkerAllowlist` JSON is written to `<infra_dir>/egress-proxy-config/<worker_ip>.json` after the IP is resolved. New modules: `docker_client.py` (adapter) and `egress_proxy.py` (lifecycle helpers). `start_worker_once` and `_run_container` in `runtime.py` accept an optional `docker_adapter` kwarg for this path.
- PR — ADR-0056 dashboard auto-deploy. `runtime.py` gains `recreate_dashboard_container` (mirror of `recreate_api_container`): runs `_ensure_images_built` for the shared rebuild path, force-removes the running `treadmill-dashboard` container, and `docker run`s a new one from the freshly-tagged `treadmill-dashboard:dev` using `_build_dashboard_service_spec` so `up` and the recreate path share one creation site. `deploy_watcher.py` gains a `services/dashboard/` → `dashboard` entry in `_DISPATCH_TABLE` (first-match wins; sits between `services/api/` and `workers/agent/`), a `_action_dashboard` action that `_sync_local_to_origin` + invokes the recreate fn, and a required `recreate_dashboard_fn` kwarg on `DeployWatcher.__init__` wired through both the dev-local and fully-local CLI entrypoints. No `docker build` shell-out from the watcher and no `/health/ready` probe — the dashboard is static nginx, the build path is owned by the runtime helper.
- PR — ADR-0053 Wave 3: `_dev_local_worker_env` now lifts `aws.corpus_s3_uri` (OPERATOR-supplied YAML field, not a CDK output) into the agent worker's `TREADMILL_CORPUS_S3_URI` env var, so `tools/load-analysis-corpus.sh pull` works from inside the worker. Absent in YAML → env var omitted entirely (the optimizer surfaces a clean configuration error rather than failing deep in boto3).
- PR — `docs pull`/`push` now scope the local mirror by repo: the effective dir is `<--dir>/<owner>/<name>/`, matching the S3 `repo-context/<repo>/` key layout so pulling several projects into one root no longer collides. `push` is a no-op when the repo-scoped subdir is absent (e.g. before first author) instead of relying on `rglob` over a missing path.
- ADR-0055 step 4 — operator surface. `build_deployment_config` (`deployment_config.py`) scans CFN outputs for values matching `treadmill-<id>/claude-account-<name>` and emits one `aws.claude_accounts` entry per — `{name, type: "oauth", secret_name}` — sorted by name. `claude_default_account` is left unset so the operator picks which account is default. `runtime.py` reads `aws.claude_accounts` from the operator YAML, serialises it as `CLAUDE_ACCOUNTS_JSON` (`{name: {type, secret_name}}`), and injects that plus `CLAUDE_DEFAULT_ACCOUNT` into the API container env. Empty/absent ⇒ API's resolver returns 503 and the worker keeps using the existing `CLAUDE_CREDENTIALS_PATH` bind-mount (no behavior change).
- PR — Closed the stale-source sibling to ADR-0024. `_action_api` and `_action_agent` now `git fetch origin` + `git merge --ff-only origin/main` in the local clone before each `docker build`, via a new `DeployWatcher._sync_local_to_origin` helper. Without this, `docker build <local_dir>` packaged whatever was on disk — so when the operator's clone was behind `origin/main` the "fresh" image was built from pre-merge source (the 2026-05-26 OTel canary observed exactly this: the gRPC→HTTP exporter fix merged but the rebuilt API image still contained the gRPC client). ff-only failure (unpushed local commits, different branch checked out) logs a warning that includes the merge stderr and falls back to building from current local state — preserving today's behavior so the sync step doesn't introduce a new silent-skip vector. All git calls are plain `subprocess.run` with `check=False` (no GitPython dep).
- PR — Fixed deploy-watcher's silent-no-op auto-deploy for the API (ADR-0024). `deploy_watcher._action_api` now (a) `docker build`s the fresh `treadmill-api:dev` image and then (b) **recreates** the container from it via a new `LocalRuntime.recreate_api_container` helper — force-removing the running container and `docker run`ning a new one with the same env/ports/network as `up`. The previous `docker restart` re-ran the EXISTING container's image, so the freshly-built bits (api code + migrations) never went live. Recreate goes through `_build_api_service_spec` (factored out of `_build_dev_local_service_specs`) so `up` and the deploy-watcher share one creation path and can't drift. The post-deploy health probe now targets the dev-local API port from the deployment YAML (`cfg["local"]["api_url"]` → `http://localhost:8088/health/ready`) instead of the hardcoded `:8000` that silently timed out against the wrong port; the YAML default in `_LOCAL_DEFAULTS` is corrected from `:8000` to `:8088` to match the runtime's `DEV_LOCAL_API_HOST_PORT` constant.
- PR — Added `treadmill-local docs` command group (ADR-0054): `list`, `get`, `pull`, `push` subcommands backed by a new `docs_sync.py` module. Functions take injected HTTP callables for testability. `pull` writes all docs to `.treadmill-docs/<doc_path>` (mkdir parents); `push` PUTs each file with `{"content": ...}` (last-write-wins). Repo is inferred from cwd git remote when `--repo` is omitted.
- PR — Scheduler spawn site in `runtime.py` (`_start_scheduler_dev_local`) now redirects `stdout`/`stderr` to `DEVNULL` and passes the log path through `TREADMILL_SCHEDULER_LOG_FILE`; the subprocess owns its rotating file via `treadmill_api.scheduler.bounded_logging.configure_rotating_logging`. Closes the unbounded-append sibling to the autoscaler/deploy-watcher fix — the 463 identical `ConnectionRefusedError` tracebacks observed during the recent dependency outage no longer can recur.
- PR — Autoscaler and deploy-watcher subprocesses now own their own log files via `subprocess_logging.configure_rotating_logging` (10 MB × 3 backup cap). Parent spawn sites in `runtime.py` redirect `stdout`/`stderr` to `DEVNULL` and pass the log path through `TREADMILL_AUTOSCALER_LOG_FILE` / `TREADMILL_DEPLOY_WATCHER_LOG_FILE`, replacing the unbounded `open(<file>, "ab")` redirect that filled a developer's disk on 2026-05-20. Persistent loop errors now log a full traceback once per signature and then collapse repeats into periodic counted summaries via `RateLimitedErrorLogger`, so a wedged credential or unreachable queue no longer dumps a fresh stack trace every iteration.
- PR — Added `treadmill-local repo onboard` (ADR-0051): infers `owner/name` from the cwd's git origin remote, builds a minimal `repo_profile` from the checkout, and POSTs to `{api_url}/api/v1/onboarding/repos`. New `onboard.py` holds the pure helpers; the CLI stays decoupled from `treadmill_api` (the endpoint owns the schema + `recommend_mode`).
- PR — Extended `treadmill-local init` to also try reading from `TreadmillObservabilityStack` CFN outputs (merged when deployed; gracefully skipped when absent). Extended `_dev_local_api_env` + `_dev_local_worker_env` in `runtime.py` to inject `OTEL_EXPORTER_OTLP_ENDPOINT` from `aws.observability_collector_endpoint` per ADR-0020.
- [#36](https://github.com/anthropics/treadmill/pull/36) — Fetches API credentials at startup and injects them into the agent container environment.
- [#34](https://github.com/anthropics/treadmill/pull/34) — Reads CDK synth output into the deployment config YAML so containers can discover each other.
- [#2](https://github.com/anthropics/treadmill/pull/2) — Initial spike: moto + Docker Compose proof-of-concept (now evolved to docker run + autoscaler).

## Pitfalls

- Moto does not implement all AWS service behaviors; changes to the API's SQS or SNS usage can fail locally but succeed against real AWS. Always validate changes against a real AWS environment before moving to production.
- Docker resource limits (memory, CPU) in adapter container specs are hints, not enforced; if containers exceed their limits locally, Docker may kill them without warning. Use `docker stats` to monitor and test load locally before deploying.
- The autoscaler reads moto's SQS state directly; if moto's SQS implementation diverges from AWS behavior (message visibility, batch operations, long polling), the autoscaler may misbehave. Monitor the adapter's logs for scale-up/down decisions during development.
- Host-side credential injection via `startup_auth.py` happens at adapter startup; if credentials rotate or become invalid mid-session, the running containers will not refresh them until the next `treadmill-local up`.

## Navigation

- **Adjacent:** `infra/` (reads CDK synth output from this app); `services/api/`, `workers/agent/` (run as containers orchestrated by this adapter).
- **Decisions:** ADR-0002 (local-first + CDK as single source of truth); ADR-0016 (dev-local deployment topology); ADR-0018 (autoscaler in dev-local mode); ADR-0019 (host-side credential injection); ADR-0060 (egress proxy + worker network isolation); ADR-0064 (multi-network attach for internal worker traffic — completes the ADR-0060 topology); ADR-0065 (real-Docker smoke gate — `scripts/smoke_boot.sh` exists; the `.github/workflows/autoscaler_smoke.yml` companion was removed 2026-06-05 as never-passing, see ADR-0065 superseded note); ADR-0072 (managed host processes read IAM credentials from file, not operator SSO).
- **Follow:** Start with ADR-0002 to understand why this adapter exists; read `runtime.py` and `autoscaler.py` to understand the startup and scaling flow.
