- (task 1518598a) **Team-scheduler hardening — default control-script path fix + durable unit.** The daemon's default `treadmill-team-control` path used `parents[3]` (the repo root) and resolved to `<repo>/cc-channels/systemd` — missing the `tools/` segment — so it exited 1 unless `TREADMILL_TEAM_CONTROL` overrode it (found during alan's ADR-0091 enable; the plan's coarse grep gate never stat'd the path). Now an extracted `default_team_control_path()` (`parents[2]`) with a regression test that STATS the real tree. Durability: new `tools/cc-channels/systemd/treadmill-team-scheduler.service` (--user unit, `Restart=on-failure` + `RestartSec=10s` + `SuccessExitStatus=143`); a flock-losing second instance exits 0 = success, so the unit never restart-loops against a live reconciler — demonstrated against the actually-running daemon. Install — THE procedure (order matters; a flock-losing unit instance exits 0 and sits INACTIVE under `Restart=on-failure`, with no auto-takeover when the manual daemon later exits): (1) stop any manual daemon first — `kill $(cat ~/.treadmill/team-scheduler.pid)` (SIGTERM; graceful stop, kernel releases the flock); (2) `cp` the unit + `systemctl --user daemon-reload && systemctl --user enable --now treadmill-team-scheduler`; (3) verify `systemctl --user is-active treadmill-team-scheduler` and the pidfile points at the unit's instance. (`Restart=always` auto-takeover was considered and rejected: it trades a permanent 10s-period refusal-churn loop whenever any second instance exists for automation of a one-time transitional state.)

  KNOWN TRANSIENT (capture-on-next-occurrence): full local-adapter suite runs intermittently failed ~3 tests twice on 2026-06-12 (both during this task family's development; both reruns of identical code were green, so no names were captured either time). On the next sighting, capture BEFORE rerunning: `uv run pytest tests/ -q -rf 2>&1 | tee /tmp/local-adapter-flake-$(date +%s).txt`, then file the named tests.
