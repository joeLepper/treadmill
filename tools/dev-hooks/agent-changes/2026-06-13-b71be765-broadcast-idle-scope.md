- **Scope [AVAILABLE] broadcast to owning coordinator only (task b71be765)**: `broadcast-idle.py` now fires ONLY for `worker-<slug>-<n>` labels (checked via `_WORKER_LABEL_RE = re.compile(r"^worker-.+-\d+$")`). Orchestrators (`treadmill-*`), coordinators (`coordinator-*`), evaluators (`evaluator-*`), and any other label class return 0 without writing anything. Previously only `coordinator-*` was excluded; every orchestrator idle tick was fanning a relay into every coordinator inbox — coordinator reported 20+ non-actionable wakes in one stretch. For a valid worker label the relay goes to the OWNING coordinator's inbox only, derived by string surgery: strip `worker-` prefix → strip trailing `-<digits>` index → prepend `coordinator-` (regex `re.sub(r"-\d+$", "", ...)` handles hyphenated owner/repo slugs like `worker-medicoderhq-medicoder-2` → `coordinator-medicoderhq-medicoder`; a positional split would truncate at the first hyphen). If the owning coordinator inbox doesn't exist, the relay write is suppressed — no fan-out fallback; a missed assignment self-heals on the next cooldown tick. Cooldown and availability-state writes are unchanged. Tests in `tests/test_broadcast_idle.py` (12, rewritten): orchestrator/evaluator noop, owning-coordinator-only delivery, hyphenated-slug resolution, bystander coordinator not touched, missing-inbox suppress + availability-still-written, cooldown unchanged. Docs updated: `tools/coordinator/README.md` (worker-availability-protocol section) and `tools/coordinator/coordinator_prompt.md` ([AVAILABLE] note).
