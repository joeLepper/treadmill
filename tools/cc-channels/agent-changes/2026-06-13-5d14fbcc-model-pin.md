- **Orchestrator model fallback in launch-session.sh (task 5d14fbcc — INCIDENT 2026-06-12)**: `launch-session.sh` now exports `ANTHROPIC_MODEL="${ANTHROPIC_MODEL:-claude-opus-4-8}"` after all per-label `.env` sourcing. This covers orchestrator labels (`treadmill-alan`, `treadmill-bert`, etc.) which carry no per-label `.env` and thus had no model pin. The `${var:-default}` form preserves any model already set (e.g. `claude-sonnet-4-6` written by `treadmill team up` into a worker's `.env`), so team-role sessions are not affected. Root cause: model-less sessions default to `claude-fable-5` (unavailable on this account) on `--resume`; the 2026-06-12 incident killed coordinator + evaluator sessions silently; workers were safe only because their cwd `.claude/settings.json` already pinned sonnet. The durable fix for coordinator/evaluator/worker is in `cli/treadmill_cli/commands/team.py` (`_env_contents` now adds `ANTHROPIC_MODEL`); this launcher change closes the gap for orchestrators. Tests: `test_orchestrator_label_carries_opus_model_fallback` and `test_team_role_env_model_not_overridden_by_launcher` in `tests/test_coordinator_launch.py` (also updated `_build_env` fake claude to record `ANTHROPIC_MODEL` for all tests).
